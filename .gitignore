/*.elc
/build/
/.dir-locals-2.el
*~
.#*

# Local development only: the manual test matrix, hot-reload helper and
# screenshot fixture generator.  Useful here, not part of what the package
# installs.  The ERT regression suite under /test is versioned.
/dev/
/TEST-MATRIX.org
/.agent-shell/
/.serena/
